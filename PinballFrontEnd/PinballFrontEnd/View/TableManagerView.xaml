<Window x:Class="PinballFrontEnd.View.TableManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PinballFrontEnd.View"
        mc:Ignorable="d"
        Title="TableManagerView" Height="300" Width="800">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="20" />
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="FontSize" Value="20" />
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Button Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="10"  Command="{Binding SaveTableList}" Content="Save Table Database" />
            <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="10" Command="{Binding OpenTableList}" Content="Open Table Database" />
            
            <!-- Grid to show main table data-->
            <DataGrid x:Name="tableDataGrid" Grid.Row="2" Grid.Column="0" Grid.RowSpan="3" ItemsSource="{Binding TableList, Mode=TwoWay}" AutoGenerateColumns="True">
                <!--<DataGrid.Columns>
                    <DataGridTextColumn Header="Table Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Playfield" Binding="{Binding Playfield}" />
                </DataGrid.Columns>-->
            </DataGrid>

            <MediaElement Grid.Row="2" Grid.Column="1" Grid.RowSpan="3" UnloadedBehavior="Close" LoadedBehavior="Play" ScrubbingEnabled="True" IsMuted="True" Source="{Binding SelectedItem.Playfield, ElementName=tableDataGrid}">
                <MediaElement.LayoutTransform>
                    <RotateTransform Angle="-90" />
                </MediaElement.LayoutTransform>
            </MediaElement>

            <MediaElement Grid.Row="2" Grid.Column="2" UnloadedBehavior="Close" LoadedBehavior="Play" ScrubbingEnabled="True" IsMuted="True" Source="{Binding SelectedItem.Backglass, ElementName=tableDataGrid}">
            </MediaElement>

            <MediaElement Grid.Row="2" Grid.Column="2" LoadedBehavior="Stop" IsMuted="True" Source="{Binding SelectedItem.DMD, ElementName=tableDataGrid}">
            </MediaElement>

        </Grid>
        
    </Grid>
</Window>
